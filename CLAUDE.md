# CLAUDE.md — 首都圏てつどうずかん

## このリポジトリの今のフェーズ

**フェーズ1：データ整備。** アプリのUI実装はフェーズAのデータが揃ってから着手する。
`data/` 配下のJSONを正確に作りきることに集中する。

最終的な対象範囲は `refs/route-map-2026-04.pdf` に載っている全路線・全駅。
ただし一度に作らず、`docs/LINE_CHECKLIST.md` の順で少しずつ進める。

**駅ナンバリングはフェーズ1に含む。** 路線を1本調べるときに駅順と駅番号を
同じページから同時に取れるので、後から全路線を調べ直すより手戻りが少ない。

## 参照資料

`refs/` に路線図と凡例がある。詳しい使い分けは `refs/README.md` を読むこと。要点だけ再掲する。

| ファイル | 何に使うか | 使ってはいけないこと |
|---|---|---|
| `route-map-2026-04.pdf` | 駅名が実在するかの照合、路線名の正式表記 | **駅の並び順の根拠にはできない**（抽出しても順序情報が無い） |
| `route-map-legend.jpg` | 事業者名、路線記号、どの路線がどの系統色か | **カラーコードの確定には使わない**（JPEG圧縮で色が濁る） |
| `jr-numbering-symbols.png` | JR各社の路線記号の参考 | 2018年時点の情報なので単独の根拠にしない |
| `map-station-names.txt` | PDFから抽出した駅名1,863件。grepでの実在照合用 | 並び順の根拠にはならない |
| `map-station-coords.json` | PDF上の駅ラベル座標。`tools/check-order.mjs` が並び順の破綻検査に使う | 順序の**証明**にはならない。おかしい並びに気づくための道具 |

**これらは参照専用。** `.gitignore` で Git 管理外にしてある。
コピー・変換・再配布はしない。中身の図版をアプリに埋め込むこともしない。

## 絶対ルール

1. **推測で駅を並べない。** 駅の順序が確信を持てない路線は、`verified: false` のまま出力し、
   不確かな箇所を `docs/LINE_CHECKLIST.md` に「要確認」として書き出す。
   でっち上げるくらいなら「わからない」と報告するほうが遥かに価値がある。
2. **1回の作業は1事業者まで。** 事業者をまたいで生成しない。
   路線数が多い事業者（JR東日本など）はさらに分割する。
3. **出典は事業者公式サイトを最優先。** 根拠URLを路線ファイルの `source` に必ず残す。
   Wikipediaは裏取りには使うが、単独の根拠にはしない。
   **特にふりがなは公式のローマ字表記から起こすこと。**
   漢字だけを見て推測すると読み間違える（妙典＝みょうでん、原木中山＝ばらきなかやま）。
4. **必ず検証を通す。** ファイルを追加・編集したら `node tools/validate.mjs` を実行し、
   エラー0を確認してから完了報告する。
5. **公式ロゴ画像を持ち込まない。** 路線記号は後工程でCSS/SVGで自前描画する。
   画像ファイルのダウンロード・埋め込みは禁止。
6. **駅マスタは統合する。** 物理的に同じ駅は事業者が違っても `stations.json` に1エントリ。
   例：上野（JR・銀座線・日比谷線）は全て `id: "ueno"`。
   一方、名前が違えば別駅として扱う（船橋 と 京成船橋 は別）。
   事業者間で字体だけが違う場合（市ケ谷／市ヶ谷）は同一駅として1エントリにし、
   差異を `docs/LINE_CHECKLIST.md` に書き出す。
7. **個人情報・外部通信なし。** データは静的JSONのみ。
8. **`verified: true` は人の判断で立てる。** AIが独断で立てない。
   立てるときは `verifiedOn`（日付）と `verifiedNote`（何をどう確かめたか）を必ず一緒に書く。
   後から「この路線は何を根拠に確認済みなのか」を追えるようにするため。

   **実際の運用（2026-09-02 時点の注記）：**
   現在 `verified: true` の173路線は、**事業者公式サイトとWikipediaの2系統照合、
   および路線図PDFとの駅名照合をAIが行った結果**として立てたものであり、
   このルールが本来求めている「人が路線図と突き合わせる最終確認」は**まだ済んでいない**。
   何をどう確かめたかは各路線ファイルの `verifiedNote` に個別に書いてある。
   限界（路線図に描かれておらず照合できなかった45路線など）は
   `docs/LINE_CHECKLIST.md` に記録してある。

## 命名規則

- 駅id：**事業者公式のローマ字表記をそのまま小文字化**する。空白はハイフンに、副称は落とす
  - `Nishi-nippori` → `nishi-nippori`、`Toranomon Hills` → `toranomon-hills`
  - 自分でヘボン式に変換しない（新橋を shimbashi にするか shinbashi にするかで揺れるため）
  - 同名で別の駅が実在する場合のみ、地域や事業者で修飾する
    （`ogawamachi-tokyo` / `ogawamachi-saitama`）
- 路線ファイル名：`{operatorId}--{路線ローマ字}.json`（ハイフン2つが区切り）
- 路線id：ファイル名から `.json` を除いたものと完全一致させる

データ構造の詳細は `docs/DATA_SCHEMA.md`。決定の経緯は `docs/DECISIONS.md`。

## 作業の型

```
1. docs/LINE_CHECKLIST.md で次に着手する事業者を1つ選ぶ
2. その事業者の公式サイトで、路線ごとに次を取る
     駅の並び順 / 駅番号 / 路線記号 / 路線カラー / 正式路線名
3. 同じ内容をWikipediaでも取り、2系統が一致することを確認する
     食い違ったら公式を採り、食い違った事実をチェックリストに書く
4. ふりがなは公式のローマ字表記から起こす
5. 駅を駅ナンバリングの01側から順に並べる
6. **並べた全駅名を refs/map-station-names.txt と照合する**
     このファイルに無い駅名を書いていたら、綴り間違いか実在しない駅。必ず潰す
7. stations.json に無い駅を追記する（ふりがな必須）
8. node tools/validate.mjs を実行してエラー0を確認
9. node tools/check-order.mjs で並び順の破綻検査を通し、指摘が出たら1件ずつ潰す
     ラベル配置による誤検知も出る。誤検知と判断した根拠は verifiedNote に残す
10. LINE_CHECKLIST.md のステータスと要確認メモを更新
11. 報告して停止する
```

※ 手順6は駅名が実在するかの確認であって、**並び順が正しいことの確認にはならない**。
並び順は人が路線図を見て確認する。ここを混同しないこと。

※ 駅数が多いときは、手打ちせず生成スクリプト経由でJSONを作ると転記ミスを防げる。
共有駅は同じ定数を参照させ、名前とふりがなの不一致をassertで弾くとよい。

## 報告のしかた

作業完了時は必ずこの3点を書く。

- 追加した路線名と駅数
- validate.mjs の結果
- **自信が持てなかった箇所**（駅の順序、支線の扱い、正式名称、ふりがな、字体の違い）

3つめを省略しない。ユーザーはここを見て確認作業を行う。
